{
    "scripts": {
        "local":"light-server -s . -b localhost",
        "build": "./build.sh",
        "test": "echo 'testing npm run to see if it's working and, if u see this, it's working'"
    },
    "dependencies": {
        "bulma": "^0.4.2",
        "light-server": "^2.0.2"
    },
    "comments":[
        "Don't forget to give build.sh the executable permission."
    ]
}
